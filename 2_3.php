<?php
    // Define a class
    class User {
        // properties (attribute)
            public $name = 'Gauresh';

        // Methods (functions)
    }

    // Instantiate a user object from the user class
    $user = new User();
    echo $user->name;